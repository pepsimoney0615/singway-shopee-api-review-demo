# momo API test environment placeholders only.
# Do not put real credentials in this file.
MOMO_API_TOKEN=replace-with-local-token-only
MOMO_API_SECRET=replace-with-local-secret-only
MOMO_API_BASE_URL=https://example.invalid/momo-api
